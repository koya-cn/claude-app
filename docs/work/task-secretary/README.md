# task-secretary

- Status: done（レビュー所見 #28 の対応を同ブランチで追加）
- Branch: `feature/task-secretary`
- Issue: なし（作成時点で関連するオープンIssueなし）

タスク秘書の第1スライス（台帳と会議収集）の作業フォルダ。

| ファイル | 内容 |
|---|---|
| `spec.md` | このスライスの要件定義 |

実装は `claude-secretary/`、収集手順書は `.claude/skills/secretary-collect-meetings/`。
テスト155件。台帳の契約（レコードの項目、更新の方針、終了コード）は
`claude-secretary/README.md` に置いたので、この spec は昇格させない。

実データで1回通し、不具合2件（コネクタの封筒でクラッシュ／自分の担当が
`self` にならない）を検出して修正した。修正後の通し確認は保存済みメモでの
再パースまでで、**コネクタから取り直しての通しは未実施**。次に収集を
走らせたときが確認になる。

後続のスライス（ブリーフィング、タスク管理サービスからの収集、セッション収集、
ボード、日次記録の代行生成）は、それぞれ別の作業フォルダを作る。
